# Configuration for i18n workflow.

locales:
    - en  # English - Source Language
    - ar  # Arabic

generate_merge:
    django.po:
        - django-partial.po
    djangojs.po:
        - djangojs-partial.po
